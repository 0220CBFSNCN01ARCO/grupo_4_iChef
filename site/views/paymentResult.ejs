<!DOCTYPE html>
<html lang="es">
    <%- include('./partials/head') %>
<body>
    <%- include('./partials/header') %>
    <!--cuerpo-->
    <div class="container-fluid mb-4">
        <div class="row headTable badge-dark" style="padding-top: 10px;">
          <div class="col">
              <a>Confirmación de pago</a>
          </div>
        </div>
    </div>
    <div class="container contPayment">
        <div class="row">
            <div class="alert alert-<%= typeMsg %> mt-2" role="alert">
                <div class="row align-items-center">
                    <div class="col-sm-12 col-md-4 col-lg-2" style="text-align: center;">
                        <p>
                            <% if( typeMsg == 'success') { %>
                            <span class="ichef-sentiment_very_satisfied iconPay"></span>
                            <% } else { %>
                                <% if( typeMsg == 'warning') { %>
                                    <span class="ichef-warning1 iconPay"></span>
                                <% } else { %>
                                    <span class="ichef-error iconPay"></span>
                                <% } %>
                            <% } %>
                        </p>
                    </div>
                    <div class="col-sm-12 col-md-8 col-lg-10">
                        <p><%= mensaje %><a class="alert-link" href="/"> Continuar comprando.</a></p>
                    </div>
                </div>
                <% if( nroOrden > 0 && typeMsg == 'success') { %>
                <p>Se genero la orden de pedido <strong>nro: <%= nroOrden %></strong>.</p>
                <p>Se envio un mail a tu casilla de correo con el detalle de la orden generada.</p>
                <% } else { %>
                <p>Se genero la orden de pedido pero quedo pendiente de pago.</p>
                <% } %>
                <h3>Gracias por su compra!!!.</h3>
            </div>
        </div>
    </div>
    <%- include('./partials/footer') %>
    <%-include('./partials/script')%>
</body>
</html>