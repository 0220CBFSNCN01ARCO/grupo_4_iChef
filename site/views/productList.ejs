<!DOCTYPE html>
<html lang="es">
    <%- include('./partials/head') %>
<body>
    <%- include('./partials/header') %>
    <div class="row headTable">
        <div class="col-7 col-md-10 col-lg-10">
            <h5>Listado productos</h5>
        </div>
        <div class="col-5 col-md-2 col-lg-2">
            <a class="btn btn-dark" href="/product/create">
                <i class="fa fa-plus"></i> Nuevo
            </a>
        </div>
    </div>
    <div class="container-fluid">
        <div class="row">
            <ul class="nav nav-tabs" id="myTab" role="tablist">
                <li class="nav-item">
                  <a class="nav-link active" id="product-tab" data-toggle="tab" href="#productos" role="tab" aria-controls="productos" aria-selected="true">Market</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" id="cajas-tab" data-toggle="tab" href="#cajas" role="tab" aria-controls="cajas" aria-selected="false">Cajas</a>
                </li>
            </ul>
        </div>
        <div class="tab-content" id="tabContentProduct">
            <div class="tab-pane fade show active" id="productos" role="tabpanel" aria-labelledby="product-tab">
                <div class="row">
                    <table class="table table-striped">
                        <thead class="thead-dark">
                        <tr>
                            <th class="titleHead" scope="col">Código</th>
                            <th class="titleHead" scope="col">Descripción</th>
                            <th class="titleHead" scope="col">Marca</th>
                            <th class="titleHead" scope="col">Precio</th>
                            <th class="titleHead" scope="col">Estado</th>
                            <th class="titleHead" scope="col">Acciones</th>
                        </tr>
                        </thead>
                        <tbody>
                            <%for(let i=0; i < productos.length; i++ ) { %>
                                <% if(productos[i].product_type_id == 1) { %>
                                    <%- include('./partials/tableProduct', {productos: productos[i]}) %>
                                <% } %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
            <div class="tab-pane fade" id="cajas" role="tabpanel" aria-labelledby="cajas-tab">
                <div class="row">
                    <table class="table table-striped">
                        <thead class="thead-dark">
                        <tr>
                            <th class="titleHead" scope="col">Código</th>
                            <th class="titleHead" scope="col">Descripción</th>
                            <th class="titleHead" scope="col">Marca</th>
                            <th class="titleHead" scope="col">Precio</th>
                            <th class="titleHead" scope="col">Estado</th>
                            <th class="titleHead" scope="col">Acciones</th>
                        </tr>
                        </thead>
                        <tbody>
                            <%for(let i=0; i < productos.length; i++ ) { %>
                                <% if(productos[i].product_type_id == 2) { %>
                                    <%- include('./partials/tableProduct', {productos: productos[i]}) %>
                                <% } %>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <%- include('./partials/footer') %>
    <%-include('./partials/script')%>
</body>
</html>