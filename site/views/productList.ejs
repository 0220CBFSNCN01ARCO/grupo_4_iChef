<!DOCTYPE html>
<html lang="es">
    <%- include('./partials/head') %>
<body>
    <%- include('./partials/header') %>
    <%- include('./partials/subtitle') %>
    <div class="row headTable">
        <div class="col-10 col-md-10 col-lg-10">
            <h5>Listado productos</h5>
        </div>
        <div class="col-2 col-md-2 col-lg-2">
            <button type="button" class="btn btn-primary add-new"><i class="fa fa-plus"></i> Nuevo</button>
        </div>
    </div>
    <div class="container-fluid">
        <ul class="nav nav-tabs" id="myTab" role="tablist">
            <li class="nav-item">
              <a class="nav-link active" id="product-tab" data-toggle="tab" href="#productos" role="tab" aria-controls="productos" aria-selected="true">Productos market</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" id="cajas-tab" data-toggle="tab" href="#cajas" role="tab" aria-controls="cajas" aria-selected="false">Cajas</a>
            </li>
        </ul>
        <div class="tab-content" id="tabContentProduct">
            <div class="tab-pane fade show active" id="productos" role="tabpanel" aria-labelledby="product-tab">
                <table class="table table-striped">
                    <thead class="thead-dark">
                    <tr>
                        <th class="titleHead" scope="col">Código</th>
                        <th class="titleHead" scope="col">Descripción</th>
                        <th class="titleHead" scope="col">Precio</th>
                        <th class="titleHead" scope="col">Acciones</th>
                    </tr>
                    </thead>
                    <tbody>
                        <%for(let i=0; i < product.length; i++ ) { %>
                            <% if(product[i].tipo == 'PRODUCTO') { %>
                                <%- include('./partials/tableProduct', {producto: product[i]}) %>
                            <% } %>
                        <% } %>
                    </tbody>
                </table>
            </div>
            <div class="tab-pane fade" id="cajas" role="tabpanel" aria-labelledby="cajas-tab">
                <table class="table table-striped">
                    <thead class="thead-dark">
                    <tr>
                        <th class="titleHead" scope="col">Código</th>
                        <th class="titleHead" scope="col">Descripción</th>
                        <th class="titleHead" scope="col">Precio</th>
                        <th class="titleHead" scope="col">Acciones</th>
                    </tr>
                    </thead>
                    <tbody>
                        <%for(let i=0; i < product.length; i++ ) { %>
                            <% if(product[i].tipo == 'CAJA') { %>
                                <%- include('./partials/tableProduct', {producto: product[i]}) %>
                            <% } %>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <%- include('./partials/footer') %>
    <%-include('./partials/script')%>
</body>
</html>